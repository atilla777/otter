# frozen_string_literal: true

RSpec.describe Otter do
  it 'has a version number' do
    expect(Otter::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
